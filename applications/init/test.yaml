apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-test
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/App-of-apps-argo-example/gitops-core.git'
    targetRevision: main
    path: applications/main
    helm:
      valueFiles:
      - values-test.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
